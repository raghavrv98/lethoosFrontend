/*
 * OffersPage Messages
 *
 * This contains all the text for the OffersPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OffersPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OffersPage container!',
  },
});
