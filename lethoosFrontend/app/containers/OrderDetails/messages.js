/*
 * OrderDetails Messages
 *
 * This contains all the text for the OrderDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrderDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderDetails container!',
  },
});
