// import { fromJS } from 'immutable';
// import { selectSamplePageDomain } from '../selectors';

describe('selectSamplePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
