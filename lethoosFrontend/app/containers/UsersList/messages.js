/*
 * UsersList Messages
 *
 * This contains all the text for the UsersList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UsersList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UsersList container!',
  },
});
