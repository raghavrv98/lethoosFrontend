/*
 * ShopDetails Messages
 *
 * This contains all the text for the ShopDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ShopDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ShopDetails container!',
  },
});
