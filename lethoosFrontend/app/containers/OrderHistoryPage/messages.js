/*
 * OrderHistoryPage Messages
 *
 * This contains all the text for the OrderHistoryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrderHistoryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderHistoryPage container!',
  },
});
