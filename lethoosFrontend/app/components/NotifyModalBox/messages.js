/*
 * NotifyModalBox Messages
 *
 * This contains all the text for the NotifyModalBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NotifyModalBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NotifyModalBox component!',
  },
});
