/*
 * MainLoader Messages
 *
 * This contains all the text for the MainLoader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MainLoader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MainLoader component!',
  },
});
