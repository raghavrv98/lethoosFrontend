/*
 * CommonModalBox Messages
 *
 * This contains all the text for the CommonModalBox component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CommonModalBox';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CommonModalBox component!',
  },
});
