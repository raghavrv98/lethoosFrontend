/*
 * OrderPlacedPage Messages
 *
 * This contains all the text for the OrderPlacedPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OrderPlacedPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderPlacedPage container!',
  },
});
